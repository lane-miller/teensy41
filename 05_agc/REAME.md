# 05_agc

Automatic gain control using a separate envelope follower (prev written in 04) and gain stage as composable audio graph nodes
Envelope follower feeds level estimate to AGC object which applies compensating gain to the audio stream